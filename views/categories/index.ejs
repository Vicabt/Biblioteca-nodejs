<div class="container-fluid py-4">
  <div class="row mb-3">
    <div class="col-12">
      <div class="d-flex justify-content-between align-items-center">
        <h2 class="fw-bold">Categorías de Libros</h2>
        <a href="/categories/add" class="btn btn-warning"><i class="fa fa-plus-circle me-2"></i>Nueva Categoría</a>
      </div>
    </div>
  </div>
  <div class="row mb-3">
    <div class="col-12">
      <div class="card">
        <div class="card-header bg-warning text-white fw-bold">Gestión de Categorías</div>
        <div class="card-body">
          <input type="text" class="form-control mb-3" placeholder="Buscar categorías nombre o estado..." id="searchInput">
          <div class="table-responsive">
            <table class="table table-bordered align-middle">
              <thead class="table-light">
                <tr>
                  <th>#</th>
                  <th>Nombre</th>
                  <th>Estado</th>
                  <th>Acción</th>
                </tr>
              </thead>
              <tbody>
                <% if (categories && categories.length > 0) { %>
                  <% categories.forEach(function(category, idx) { %>
                    <tr>
                      <td><%= idx + 1 %></td>
                      <td><%= category.name %></td>
                      <td><%= category.state == 1 ? 'Activo' : 'Inactivo' %></td>
                      <td>
                        <a href="/categories/edit/<%= category.id_category %>" class="btn btn-sm btn-primary me-1"><i class="fa fa-edit"></i> Editar</a>
                        <a href="/categories/delete/<%= category.id_category %>" class="btn btn-sm btn-danger"><i class="fa fa-trash"></i> Eliminar</a>
                      </td>
                    </tr>
                  <% }); %>
                <% } else { %>
                  <tr><td colspan="4" class="text-center">No hay categorías registradas.</td></tr>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div> 
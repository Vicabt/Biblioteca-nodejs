<div class="container-fluid py-4">
  <div class="row mb-3">
    <div class="col-12">
      <div class="d-flex justify-content-between align-items-center">
        <h2 class="fw-bold">Autores</h2>
        <a href="/authors/add" class="btn btn-info"><i class="fa fa-plus-circle me-2"></i>Nuevo Autor</a>
      </div>
    </div>
  </div>
  <div class="row mb-3">
    <div class="col-12">
      <div class="card">
        <div class="card-header bg-info text-white fw-bold">Gestión de Autores</div>
        <div class="card-body">
          <input type="text" class="form-control mb-3" placeholder="Buscar autores nombre o estado..." id="searchInput">
          <div class="table-responsive">
            <table class="table table-bordered align-middle">
              <thead class="table-light">
                <tr>
                  <th>#</th>
                  <th>Nombre</th>
                  <th>Estado</th>
                  <th>Acción</th>
                </tr>
              </thead>
              <tbody>
                <% if (authors && authors.length > 0) { %>
                  <% authors.forEach(function(author, idx) { %>
                    <tr>
                      <td><%= idx + 1 %></td>
                      <td><%= author.name %></td>
                      <td><%= author.state == 1 ? 'Activo' : 'Inactivo' %></td>
                      <td>
                        <a href="/authors/edit/<%= author.id_author %>" class="btn btn-sm btn-primary me-1"><i class="fa fa-edit"></i> Editar</a>
                        <a href="/authors/delete/<%= author.id_author %>" class="btn btn-sm btn-danger"><i class="fa fa-trash"></i> Eliminar</a>
                      </td>
                    </tr>
                  <% }); %>
                <% } else { %>
                  <tr><td colspan="4" class="text-center">No hay autores registrados.</td></tr>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>